# 検証記録

## v0.1.12 設定画面の構成

隔離データとポート25572を使い、WPFの25画面を両テーマで生成・レイアウト検証。アプリ設定の専用画面、サーバー設定とMOD・プラグインの画面内ナビゲーション、サーバー名の右クリックから削除画面への遷移、既存のMOD設定・server.propertiesの保存と再表示を確認しました。コア53件も成功。実サーバーは起動せず、既存のJavaプロセスには触れていません。画面画像を確認後、テスト用プレビューをWindowsのゴミ箱へ移しました。

## v0.1.11 画面・バックアップ・一覧

コア53件、WPFの独立23ページを両テーマで生成・レイアウト検証。バックアップの進捗、途中キャンセル時の.partial削除、無圧縮ZIPの復元、サーバーフォルダ消失時の登録整理、登録だけの削除を検証しました。Minecraft 1.21.1 Vanillaを隔離フォルダ・127.0.0.1:56710で起動し、クライアント入室、バックアップ復元後の再入室に成功。既存Javaは停止していません。速度測定と画面構成は[UI_AND_BACKUP.md](UI_AND_BACKUP.md)を参照。

## v0.1.10 名称変更

改名前後の配布ファイル名の選択テストを追加。内部の保存先・実行識別子は保持し、CIでは実際のv0.1.9を導入してから新版に上書きし、表示名・旧ショートカットの置換・既存データの保持を検証します。命名の予備調査は[NAME_RESEARCH.md](NAME_RESEARCH.md)を参照。

## v0.1.9 日本語設定と階層

Core49件、WPF全13画面・両テーマの検証に成功。Minecraft 1.21.1がテスト環境で生成した全設定キーに日本語名があること、ローダー選択の表示と保存値の分離、階層ボタンの切替、MOD設定GUIの保存・再表示を確認しました。JSONの日本語を含む位置計算、配列・入れ子、未知の内容の保持、TOMLのコメント保持、不正数値・保存競合の拒否も検証しています。

Fabric・Forge・NeoForge・Paperの隔離環境で、GUI設定変更後のMOD有効・無効・プリセット復元、計12回の実起動・クライアント入室が成功。テスト開始時・終了時の既存Javaプロセスは0でした。今回の実ゲームテストは既存の起動・設定・MOD運用経路の回帰確認で、すべてのMOD設定項目の意味をゲーム内で検証したものではありません。

新しいMOD設定GUIは登録済み項目のみ日本語名を持ちます。未登録キーや複雑な形式の範囲は[JAPANESE_SETTINGS.md](JAPANESE_SETTINGS.md)に明記しています。

## v0.1.8 サーバー設定GUI・更新

Core 45件、両テーマのWPF画面検証に成功。propertiesのJava形式エスケープ、継続行、コメント・未知項目、重複キー、入力検証、保存競合、プロファイル保存失敗時の巻き戻しを確認しました。Fabric・Forge・NeoForge・Paperの隔離環境でGUIから保存・再表示し、MOD有効・無効・プリセット復元の計12回の実起動・入室に成功。利用中のサーバーは対象外です。

GitHub Releasesの実APIでv0.1.7インストーラー56,937,806 bytesの取得とSHA256照合に成功。コアテストで古い版・draft・不完全な配布の除外、チェックサム重複・サイズ不一致・改変を確認。インストーラーCIには親プロセス終了待ち、改変拒否、検証済みインストーラー適用、データ保持を追加しています。

更新ヘルパーの初回CIでは、子Windows PowerShellが継承したモジュール検索パスによりGet-FileHashを見つけられず失敗しました。.NETのSHA256ストリーム計算へ変更し、このPCでも検証済みインストーラーの再適用とprofiles.jsonのハッシュ保持を確認しました。

AutoModpackの実クライアント同期と全MOD・全バージョンの組合せは未検証です。

## v0.1.7 起動表示の検証

初回描画後の読み込み開始、待機中のDispatcher応答、メイン画面への切り替え、待機中に閉じた場合に画面が再表示されないこと、読み込み失敗の表示をWPFで検証しました。UIテストは独立したApplicationと製品のスタイルを使い、実データを読むApp.OnStartupは実行しません。

このPCで隔離データを使い、旧インストール版は最初のウィンドウまで550/527/539ms、新しいReadyToRun版は996/459/426msを観測しました。新版のメイン画面検出は1877/616/564msです。プロセス開始からウィンドウハンドルの検出までの計測で、クリック遅延やピクセルの描画完了を測るものではありません。OSキャッシュは消去しておらず、初回検査を含む影響もあるため、すべての起動が速くなるという保証ではありません。最初の非ReadyToRun版では2563msの初回表示も観測しました。

Core 40件と既存の画面・テーマ・ローダーのUIテストも成功。サーバーの自動起動は追加していません。


## v0.1.6 インストーラー検証

Windowsで実インストール・同じ版の上書きインストール・アンインストールに成功。起動ショートカットのリンク先、Windowsアプリ一覧の登録と削除、データ領域の一時マーカーと既存profiles.jsonのハッシュ保持を確認しました。実行中アプリを表すmutexを保持した場合、インストールとアンインストールが拒否され、アプリのファイルを削除しないことも確認しています。サーバーは起動・停止していません。Core 40件・WPFテストも成功。

GUIウィザードの全画面操作やコード署名の信頼表示は未検証です。実動作検証はサイレントモードで行っています。


## v0.1.5 追加検証

Core 40件、WPFテスト、5構成の隔離サーバーで14回の起動・入室に成功。[MOD設定保持の検証記録](FEATURE_VALIDATION.md)と[対応範囲](MOD_CONFIGURATIONS.md)を参照。

## v0.1.3 追加検証

[MOD・設定の実機検証](FEATURE_VALIDATION.md) に実行条件、構成ごとの結果、修正した問題、残る未検証範囲を記録しています。従来の取得・レイアウト検証に加え、画面の保存・切替・起動停止処理と実際のMinecraftクライアント接続を組み合わせました。

## v0.1.2 追加検証

Vanilla・Paper・Fabric（Fabric API入り）・Forge・NeoForgeを隔離環境で実行し、5種類すべてで初回・再起動・復元後の3回の起動とクライアント入室に成功しました。コマンド、チャット往復、チャンク受信、保存ブロックの再読込、バックアップの巻き戻しを確認しています。[条件と手順の詳細](REAL_SERVER_VALIDATION.md)。

実検証で発生したWindowsの一時ファイルロックによる復元失敗を修正し、実サーバーで再検証しました。コア25件、全8ローダーの選択・保存・再表示、全10画面の両テーマ・配色・設定保持・アイコンのUIテストも成功しています。

以下は各リリース当時の記録です。「未検証」の記述も当時の状態を示します。

## v0.1.1 追加検証

コア23件、全8種類のローダー切替・保存・再表示、全10画面を両テーマでレイアウト、選択欄とポップアップの配色、即時背景切替、テーマ保存・再読込、確認ダイアログ配色と既定の「いいえ」、256pxアイコンの読み込みを確認しました。ダーク／ライトの起動設定画面を画像で確認しています。

Windows提供のファイル・フォルダ選択画面はアプリテーマの対象外です。タイトルバーはWindows 11のDWM APIを使用し、未対応環境ではOS標準へフォールバックします。今回も既存Minecraftサーバーを操作していません。

以下はv0.1.0時点の検証記録です。

検証環境: Windows 11 x64、.NET SDK 10.0.400。2026-09-07に実行。

## ローカル自動検証

- Releaseビルド: 警告0・エラー0。
- コア22項目: 日本語保存、破損JSON保護、パス境界、メモリ/ポート値、引数分離、Forge引数ファイル、properties更新、バックアップ/復元、ZIP slip、展開上限、重複ZIP、HTTPS限定、検証済み取得、ハッシュ不一致、HTTPエラー、キャンセル、ログ上限、EULA/JAR前提、ポート競合、子プロセス制御、mrpack取り込み、既存データ拒否。
- UI: 概要・コンソール・起動設定・MOD/パック・設定・バックアップ・Java・システム・ガイドの9画面を各2回生成・レイアウト。レンダリング画像を目視確認。
- 模擬子プロセス: stdout/stderr、コマンド送信、通常停止、二重起動拒否、再起動を確認。

## 実サービスとの接続検証

| 項目 | 結果 |
|---|---|
| Mojangリリース一覧 | 102件を取得 |
| Vanilla 1.21.1 | 51,627,615 bytes取得、SHA1一致、Java要件21 |
| Paper 1.21.1 | 49,394,394 bytes取得 |
| Fabric 1.21.1 | 181,840 bytesのランチャー取得 |
| Modrinth検索 | ferritecoreの結果を取得 |
| Modrinth導入 | fabric-apiを解決・SHA512検証して配置 |
| Java 21 | Temurin JRE取得・SHA256検証・展開・`-version`成功 |

初回LiveTestsでは終了直後のjava.exe一時ロックによりテスト用フォルダ削除が失敗しました。Windowsの終了後ロックを考慮した短いリトライをテストの後片付けに追加し、再実行は終了コード0でした。

## 検証範囲の限界

既に遊んでいるMinecraftサーバーを停止・再起動・変更していません。実ワールドでの長時間稼働、実プレイヤー接続、全MODパックの組み合わせ、Folia・Forge・NeoForgeの本体実行、全Java世代、インターネット公開、Windows ARM64は未検証です。

Minecraft EULAへの同意を自動で行っていないため、本検証のサーバー制御は模擬子プロセスを使用しています。取得できたことと、任意のサーバー構成でゲームプレイできることは別です。初回の実運用では新しいワールドとバックアップを使って接続確認してください。
